spring:
  application:
    name: springdatajpademo
  datasource:
    url: jdbc:postgresql://localhost:5432/workshops
    username: postgres
    password: Mg505!9405
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

